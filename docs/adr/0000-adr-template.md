# ADR NNNN: <short title, present tense>

**Status:** Proposed | Accepted | Rejected | Superseded by ADR-XXXX
**Date:** YYYY-MM-DD
**Decider:** <name or role>

## Context

<!-- What problem are we solving? What forces are in play — a porting finding, an
a11y criterion, a repeated bug, a new component earning its place? 2–4 sentences.
State the situation, not the answer. -->

## Decision

<!-- What we chose. A few sentences. For a component-idea ADR: state that the
component is in scope and draw its scope boundary in one line. -->

## Considered alternatives

1. **<Alternative A>** — short description. Rejected because …
2. **<Alternative B>** — short description. Rejected because …
3. **<The one we chose>** — short description. Chosen because …

<!-- For a component idea, "alternatives" are the adjacent options: compose from an
existing component, defer it, fold it into another contract, or leave it to the
consuming project. Say why this component earns its own contract instead. -->

## Consequences

### Positive
- …

### Costs
- …

### Risks to manage
- …

<!-- For a component-idea ADR, also record the boundary up front so the eventual
contract inherits it:
### Non-goals
- …
### Kernel dependencies
- <primitive> — why it composes it, or "none — self-contained"
-->

## Reconsider when

<!-- The trigger that reopens this decision. A condition, not a date where possible —
e.g. "when the Popover API baseline lets us delete the @supports fallback", "if a
third field needs this maths, promote it to the kernel", "when a real port hits X". -->

## References

- <Links to the component contract (`<Name>.md`), kernel module, other ADRs, PRs/commits, atomica11y criteria, external source>
